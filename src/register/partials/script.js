export let nome = document.getElementById("nome");
export let email = document.getElementById("email");

import { createUser } from "../../../modules/express";

function registerUser() {
  createUser;
}
